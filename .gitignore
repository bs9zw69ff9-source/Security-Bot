.env

# deploy.sh process management
guardian-bot.pid
guardian-bot.log

# runtime state (SQLite DB + WAL/SHM + forensic log)
guardian.db
guardian.db-wal
guardian.db-shm
security_log.jsonl

# legacy JSON state files (migrated into guardian.db on first boot)
antiping.json
mutedroles.json
warnings.json
guildsettings.json
guild_snapshot.json
failsafe_backup.json

# Rust build artifacts
rust/target/
